let option = "chat";
let attempt = 0;
while(option.toUpperCase() !== "BENZEN" && option.toUpperCase() !== "BENZEN ") {
    attempt++;
    option = prompt("Chất này là chất gì?");
}
    
alert("Chúc mừng chị gái học hóa đã hoàn thành sau " + attempt + " lượt!!!");
alert("Chị hãy vào link này để nhận nhiệm vụ tiếp theo nhaaaa!");